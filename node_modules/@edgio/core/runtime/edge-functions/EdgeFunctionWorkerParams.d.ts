/// <reference types="node" />
import { Origins } from '../../types';
import Request from '../Request';
export default class EdgeFunctionWorkerParams {
    readonly request: any;
    readonly path: string;
    readonly envVars: Map<string, string | Uint8Array>;
    readonly usrVars: Map<string, string>;
    readonly httpVars: Map<string, string>;
    readonly origins: Origins[];
    constructor(request: Request, rawBody: Buffer | undefined, path: string, envVars: Map<string, string | Uint8Array>, usrVars: Map<string, string>, httpVars: Map<string, string>, origins: Origins[]);
}
